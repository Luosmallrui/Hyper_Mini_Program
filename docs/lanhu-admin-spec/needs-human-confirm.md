# Needs Human Confirm

更新时间：2026-05-29  
确认人：用户  
结论：本轮 23 项确认项均已按建议确认；后端接口契约、错误码和联调环境由后端负责人提供。

## 已确认决策

### 蓝湖 / 范围

1. `update_time` 可临时代替上传时间使用。
   - 决策：可以。
   - 执行口径：仅作为页面筛选和排序依据，不当作严格上传时间。

2. 5 月退款、审核、已使用等页面是否纳入本轮后台管理闭环。
   - 决策：本轮只纳入审核状态页。
   - 执行口径：退款、已使用先不纳入后台管理 MVP。

### 路由和页面结构

3. 后台管理是否保持一个注册页面 `pages/user-sub/organizer/index`，内部 state 切换多个视图。
   - 决策：保持。
   - 执行口径：不新增独立后台管理路由。

4. 核销页 `verify` 是否需要独立注册路由。
   - 决策：暂不需要。
   - 执行口径：继续作为 organizer 内部视图。

### 接口和字段

5. 后台真实接口未完成，第一轮是否明确 mock-first。
   - 决策：是。
   - 执行口径：第一轮不接真实后端，只完成前端 mock 交互闭环。

6. dashboard/list/create/upload/ticket/verify/verifier/withdrawal 真实接口路径是否先不定。
   - 决策：先不定。
   - 执行口径：全部标记为 proposed/TODO，不能当作 existing 接口调用。

7. 状态枚举是否先采用 `draft/pending/approved/up/down/ended/rejected`。
   - 决策：前端 mock 可先用。
   - 执行口径：后端完成后再做字段映射。

8. 分页字段是否先采用 `page/pageSize/hasMore/total`。
   - 决策：是。
   - 执行口径：mock adapter 先使用这套分页字段。

9. 核销错误码是否先采用 `invalid_code/reverify_code/out_of_time`。
   - 决策：mock 阶段可用。
   - 执行口径：真实错误码以后端负责人提供的契约为准。

10. adapter 文件位置是否采用 `src/pages/user-sub/organizer/adapter.ts`。
    - 决策：是。
    - 执行口径：使用 organizer 局部 adapter，后续接真实接口时集中替换 adapter 内部实现。

### 权限

11. 真实权限体系未完成时，是否只保留 `isMerchantUser` / debug 判断，不扩展复杂 RBAC。
    - 决策：是。
    - 执行口径：不编造权限体系；复杂按钮权限保留 TODO。

### 表单校验

12. 活动名称、分享标题长度是否先沿用当前实现：活动名称 80，分享标题 20。
    - 决策：是。
    - 执行口径：后续如产品调整，再修改规格和实现。

13. 上传文件是否先按设计提示 2M，比例/压缩/裁剪只做前端提示和 TODO。
    - 决策：是。
    - 执行口径：第一轮仅做提示和 mock 上传，不做复杂压缩/裁剪策略。

14. 票券价格、库存、限购、观演人数是否先做最小校验：非空、数字、非负。
    - 决策：是。
    - 执行口径：复杂规则等待产品/后端确认。

15. 提现账号/银行卡/收款人是否先做非空校验，不做实名/银行卡强校验。
    - 决策：是。
    - 执行口径：实名校验、银行卡格式和脱敏规则等待后端契约。

### 资源

16. 蓝湖资源是否迁移到 `src/assets/images/lanhu-admin/`。
    - 决策：暂不迁移。
    - 执行口径：先保留 docs 资源；真正页面引用时再迁移必要资源。

17. 底部图标、导航胶囊、状态栏是否用代码/原生实现，不用切图像素级还原。
    - 决策：是。
    - 执行口径：优先代码/组件/原生实现，不使用切图强行还原。

### 交付策略

18. 第一轮是否只完成 P0 MVP，不一次性做完 21 个页面。
    - 决策：是。
    - 执行口径：优先 P0 闭环，P1/P2 分批推进。

19. 小程序真实业务资源引用目录是否用 `src/assets/images/lanhu-admin/`。
    - 决策：仅当页面实际引用时使用。
    - 执行口径：资源迁移按需进行，不预先大规模迁移。

20. P0 活动中心分页第一轮是否允许 mock/local filter。
    - 决策：允许。
    - 执行口径：第一轮分页、搜索、筛选、日期筛选均可基于 mock/local data。

21. 活动发布第 5 步/最终提交审核是否纳入本轮。
    - 决策：纳入最小提交审核 mock 闭环。
    - 执行口径：不做复杂资质审核，只保证表单可提交、mock 成功反馈、列表刷新。

22. 是否允许 Claude Code 新增 organizer 局部 adapter 文件。
    - 决策：允许。
    - 执行口径：允许新增/维护 `src/pages/user-sub/organizer/adapter.ts`，但不修改全局 request 封装。

23. 正式接口契约、错误码和联调环境由谁提供。
    - 决策：后端负责人。
    - 执行口径：Claude Code 不得凭空编造真实接口路径、错误码或后端业务规则。

## 仍需后续确认

以下内容不阻塞第一轮 mock-first 前端实现，但在接入真实后端前必须由后端负责人补齐：

- 正式后台接口路径。
- 请求方法、请求参数、响应结构。
- 后端状态枚举与前端 mock 枚举的映射。
- 核销真实错误码及错误文案。
- 分页字段是否沿用 `page/pageSize/hasMore/total`。
- 上传文件真实大小、比例、压缩、裁剪规则。
- 提现实名校验、银行卡校验、账号脱敏规则。
- 真实角色、权限、按钮可见性、危险操作权限。

## 用户中心追加确认

以下问题来自 `visual-review-cc4-user-profile-2026-06-01.md`，不阻塞前端 mock-first 修复，但需要产品或后端在后续联调前确认：

1. `/api/v1/user/info` 是否支持写入 `signature` 字段。
   - 当前建议：第一轮按 `signature` 做本地回写和 TODO，避免改全局 request。

2. 用户主页第 5 个入口的主办方状态字段最终以哪个字段为准。
   - 当前建议：先兼容 `is_merchant` / `merchant_id`；如后端提供 `organizer_status`，再映射 `settle/pending/rejected/approved`。

3. 未入驻用户点击 `我要入驻` 后最终进入哪个正式路由。
   - 决策更新：第一轮进入入驻申请表单，不要只 toast。
   - 当前建议：复用 `pages/user-sub/organizer/index?view=settlementApply&source=userEntry`，不新增 app 路由。

4. 审核中/审核未通过时，第 5 个入口文案是否仍显示 `我要入驻`，还是显示 `审核中` / `重新入驻`。
   - 当前建议：产品未确认前保留 `我要入驻`，点击进入状态详情。

5. 个人资料编辑弹窗是否保留右上角关闭按钮。
   - 当前建议：蓝湖无明显关闭按钮；若为小程序可用性保留，需作为产品适配记录。

## 用户入口与核销追加确认

以下问题来自 `user-entry-and-settlement-spec.md`：

1. 入驻申请表单最终字段。
   - 当前状态：待黄总确认。
   - 当前建议：第一轮使用 organizer 局部 mock 配置字段，不把 proposed 字段当最终接口契约。

2. 用户主办方状态字段。
   - 当前状态：待后端确认。
   - 当前建议：第一轮兼容 `is_merchant` / `merchant_id`，如后端提供 `organizer_status` 再统一映射。

3. 用户核销员身份字段。
   - 当前状态：待后端确认。
   - 当前建议：第一轮用 mock 字段 `isVerifier` / `verifierStatus` 控制入口展示。

4. `订单核销` 入口展示范围。
   - 决策：入口是用户主页左上角 `扫一扫`，只对已开通核销员身份用户展示/可用，普通用户没有。
   - 待确认：inactive/禁用核销员是否置灰还是隐藏。

4.1. 用户主页左上角客服入口承接方式。
   - 决策：蓝湖左上角应有 `扫一扫` + `客服` 两个图标，客服入口需要补齐。
   - 当前建议：第一轮优先用微信小程序原生客服能力或项目已有客服承接；若暂未配置，Toast `客服功能待接入` 并保留 TODO。
   - 待确认：正式客服使用微信原生 `contact`、企微客服、H5 客服页，还是项目内客服页面。

5. `核销记录` 入口展示范围。
   - 决策：可以在用户主页功能区增加 `核销记录` 入口。
   - 当前建议：仅 active 核销员展示；主办方但非核销员是否展示待确认。

6. `核销记录` 最终承接页面。
   - 当前建议：优先复用 `pages/user-sub/organizer/index?view=verifyRecords&source=userNav` 或 verify 页已核销列表，不新增 app 路由。
   - 待确认：后续是否需要独立核销记录页面。

7. 扫码后核销交互。
   - 当前建议：扫码后进入/停留 `verify` 页面并展示 success/failed/invalidCode 状态。
   - 待确认：是否允许扫码后直接弹出结果，不进入完整核销页。

## Claude Code 执行提醒

- 第一轮必须 mock-first。
- proposed 接口不能当作 existing 接口调用。
- 不允许修改全局 request 封装。
- 不允许凭空编造真实后端接口路径。
- 不允许把 mock 逻辑散落到大量页面里。
- 推荐使用 organizer 局部 adapter + mock 数据；后端完成后只替换 adapter。
