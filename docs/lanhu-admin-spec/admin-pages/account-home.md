# 后台主页

## 页面用途

账户 tab 的主页面，用于展示主办方资料、认证状态、基础信息入口和账户信息入口。

## 蓝湖来源

- 蓝湖索引：9
- 蓝湖页面名：`后台主页`
- 蓝湖 ID：`c4b3fa43-0173-4182-99a2-4a971234c990`
- 更新时间：`2026-05-25T16:25:24.378277Z`

## 当前问题

当前代码已经有 `src/pages/user-sub/organizer/account/index.tsx`，但主结构未按蓝湖实现：

- 当前多了 `付款信息 / 付费记录`，蓝湖账户主页没有该 section。
- 当前把 `修改密码 / 退出登录` 放在单独 `安全性` section，蓝湖要求它们在 `账户信息` 卡片内。
- 当前 row 带右侧 chevron，蓝湖导出的账户主页 HTML/CSS 未包含 row chevron。
- 之前规格包只写了 `account-withdrawal-info` 和 `account-withdrawal-edit`，缺少账户主页本身，导致 Claude Code 容易只实现提现弹窗而忽略主页面。

## 页面结构

严格按蓝湖 index 9：

1. 顶部自定义导航：`管理后台`。
2. 主办方信息卡：
   - Logo：112x112，圆角 12，边框 `rgba(151,151,151,1)`。
   - 名称：`POWER FLOW`。
   - 认证状态：`已认证`，颜色 `rgba(0,219,126,1)`。
   - 三个统计：`入驻天数 210`、`当前等级 LV1`、`服务费比例 5%`。
3. `基本信息` section：
   - `主办方编辑`
   - `主办方区域`
4. `账户信息` section：
   - `认证信息`
   - `提现信息`
   - `修改密码`
   - `退出登录`
5. 底部内部导航：`首页 / 活动 / 更多 / 账户`，账户激活。

## 禁止偏差

- 禁止出现 `付款信息` section。
- 禁止出现 `付费记录` row。
- 禁止出现单独的 `安全性` section。
- 禁止把 `修改密码` 和 `退出登录` 移出 `账户信息` section。
- 禁止把 proposed 接口当真实接口调用。
- 禁止为了功能扩展牺牲蓝湖视觉结构。

## 交互说明

- 点击 `提现信息`：打开 `account-withdrawal-info` 弹窗。
- 点击 `退出登录`：保留当前二次确认能力，但不要改变蓝湖主结构。
- 点击 `主办方编辑`、`主办方区域`、`认证信息`、`修改密码`：目标页面未确认，第一轮可用 TODO toast 或保留无跳转，并记录 need-human-confirm。

## Claude Code 实现建议

1. 先修改 `SETTING_GROUPS`：只保留两个 group。
2. `基本信息` rows：`主办方编辑`、`主办方区域`。
3. `账户信息` rows：`认证信息`、`提现信息`、`修改密码`、`退出登录`。
4. 删除 `付款信息/付费记录` 和 `安全性` group。
5. 账户主页面视觉验收通过后，再检查提现查看/编辑弹窗。
6. 本轮仍然 mock-first，不接真实后端。

## 验收标准

- 首屏与蓝湖 index 9 一致，不出现额外 section。
- 账户信息卡片包含四行：认证信息、提现信息、修改密码、退出登录。
- 点击提现信息能打开提现查看弹窗。
- 微信开发者工具截图保存并与蓝湖 index 9 对比。

## unknown / need-human-confirm

- 主办方编辑目标页面。
- 主办方区域目标页面。
- 认证信息目标页面。
- 修改密码目标页面。
- 行右侧是否允许保留 chevron；蓝湖 HTML/CSS 没有 chevron，严格视觉验收应移除。
