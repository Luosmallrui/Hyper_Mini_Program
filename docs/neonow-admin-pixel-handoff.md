# 此刻霓虹管理后台像素级交付文档

采集时间：2026-05-13 20:48 CST

本文档给不支持视觉的 agent 使用。所有尺寸以当前微信 Mac 小程序窗口截图为依据，用 `rpx` 描述时按 `750rpx = 屏幕宽度`。如果实现使用 CSS px，可按 `2rpx = 1px` 粗略换算。

重要视觉约束：本文复刻 `此刻霓虹NeoNow` 的功能结构、字段、交互、校验和信息层级；视觉风格必须跟我们当前小程序的 `管理后台` 保持一致。后文出现的黄绿色描述是实机观察，不代表要把本项目全局换成 `此刻霓虹` 品牌色。实现时按当前 `/Users/zijian_nong/Desktop/code/Hyper_Mini_Program/src/pages/user-sub/organizer/index.scss` 的 `$organizer-*` token 映射。

## 1. 可信度标记

文档中的标记含义：

- `已确认`：2026-05-13 实机看到。
- `部分确认`：实机看到主要结构，但没有完整进入所有状态。
- `待确认`：未能实机确认，不能作为复刻依据。

## 2. 全局视觉系统

### 2.1 页面

- 页面背景沿用当前管理后台：`$organizer-bg: #000000`。
- 原生导航栏背景沿用当前管理后台：`$organizer-nav: #14131A`。
- 原生导航标题：`管理后台`，白色，居中。
- 左上角返回箭头：白色线性箭头。
- 右上角微信胶囊：系统绘制，不在业务代码中实现。

### 2.2 当前项目色板

实现以当前管理后台色板为准，不新建 `NeoNow` 色板。

当前 token：

```scss
$organizer-bg: #000000;
$organizer-nav: #14131A;
$organizer-card: #181818;
$organizer-card-2: #202322;
$organizer-panel: #242424;
$organizer-input: #050505;
$organizer-border: #8A8A8A;
$organizer-border-soft: rgba(255, 255, 255, 0.10);
$organizer-text: #FFFFFF;
$organizer-text-secondary: #A0A0A0;
$organizer-text-muted: #747474;
$organizer-text-disabled: #5E5E5E;
$organizer-danger: #FF3150;
$organizer-danger-2: #FF405A;
$organizer-success: #35D34A;
$organizer-calendar-active: #D8FF4F;
$organizer-white-button: #FFFFFF;
$organizer-gray-button: #5A5A5A;
$organizer-bottom-tab: #191919;
```

映射规则：

- `此刻霓虹` 实机里的黄绿色 CTA，在本项目中不要直接照搬；主操作和 FAB 默认继续用 `$organizer-danger`。
- 发布流程 `下一步 / 提交审核` 默认继续使用当前 `.white-pill-button` 的白底黑字，除非现有代码已针对某个子控件使用 `$organizer-calendar-active`。
- 日历选中、日期 active、局部强调等已存在的黄绿色场景可继续用 `$organizer-calendar-active`。
- 错误、审核失败和危险动作继续使用 `$organizer-danger`，不要因为实机黄绿色而取消现有红色语义。

### 2.3 字体

- 使用系统字体，不引入字体文件。
- 大标题如 `已上架活动`、`活动数据`：约 `32rpx`，白色，`600`。
- 分段控件文字：约 `28rpx`，active 颜色按当前 tab 样式，inactive 灰色。
- 搜索 placeholder：约 `28rpx`，灰色。
- 空态文字：约 `36rpx`，白色，`600`。
- 按钮文字：约 `30rpx`，`600`，颜色按现有按钮类型决定。
- 底部 tab 文案：约 `24rpx`。

### 2.4 圆角

- 大卡片：`20rpx` 到 `24rpx`。
- 分段控件外框：`18rpx`。
- active 分段块：`18rpx` 到 `20rpx`。
- 搜索框/筛选按钮：`16rpx`。
- 普通按钮：`16rpx` 到 `18rpx`。
- 悬浮圆按钮：圆形。

## 3. 底部主 Tab

状态：已确认。

固定在屏幕底部。

- 背景：`#191919`。
- 高度：约 `150rpx` 加安全区。
- 四等分：`首页 / 活动 / 更多 / 账户`。
- icon：线性图标，约 `44rpx`。
- inactive icon：灰色 `#8A8A8A`。
- inactive icon：灰色。
- active icon 当前代码使用白色，active 文案使用灰色 `#8A8A8A`；保持当前管理后台风格。

特殊说明：

- 发布活动流程中底部 tab 仍可见，active 保持 `活动`。
- 不要为贴近 `此刻霓虹` 把当前底部 tab 和 FAB 全局改成黄绿色；只在功能结构上对齐。

## 4. 首页

状态：已确认。

### 4.1 顶部卡：已上架活动

位置：

- 位于导航栏下方约 `22rpx`。
- 左右外边距约 `15rpx` 到 `30rpx`，卡片接近满宽。

样式：

- 背景：近黑 `#050505`。
- 圆角：约 `24rpx`。
- 高度：约 `400rpx`。
- 顶部左侧 icon：三横列表线性 icon，灰白色。
- 标题：`已上架活动`，白色，约 `36rpx`，粗体。
- 右上角：斜向箭头 `↗`，灰白色，约 `42rpx`。
- 空态：`暂无活动`，白色，居中，约 `38rpx`，粗体。

### 4.2 活动数据卡

位置：

- 与上一个卡片间距约 `26rpx`。

样式：

- 背景：近黑，圆角与上方卡一致。
- 顶部左侧柱状图 icon。
- 标题：`活动数据`。
- 右上角斜向箭头。
- 小标题：`数据明细`，灰色。
- 小标题下有分隔线。

三列指标：

1. `今日订单`，数值 `0.00`
2. `今日销售`，数值 `0.00`
3. `专区订阅量`，数值 `0.00`

数值：

- 黄绿色。
- 数值字号约 `48rpx`。
- `0.00` 使用更宽的数字视觉。

### 4.3 快速配置

状态：已确认，2026-05-13 21:13 补采确认完整列表。

标题：`快速配置`，灰白色。

已确认卡片共 6 个，纵向排列：

1. `新增活动`
   - 左侧方形描边 icon 容器，内部 plus。
   - 主文案白色。
   - 副文案：`在此处上架你最新的活动`。
   - 右侧 chevron。
2. `添加核销员`
   - 左侧方形描边 icon 容器，内部 user-plus。
   - 副文案：`快速添加核销员`。
3. `活动抽奖`
   - 左侧方形描边 icon 容器，内部 gift。
   - 副文案：`配合活动抽奖，增加社群活跃`。
4. `活动分销`
   - 左侧方形描边 icon 容器，内部 share-nodes。
   - 副文案：`加速您的活动传播`。
5. `活动合集`
   - 左侧方形描边 icon 容器，内部 bell。
   - 副文案：`巡演必备，多场次集合展示`。
6. `赠票配置`
   - 左侧方形描边 icon 容器，内部 settings gear。
   - 副文案：`一键赠票，方便快捷`。

卡片样式：

- 背景：`#181818`。
- 边框：弱灰。
- 圆角：约 `20rpx`。
- 高度：约 `160rpx`。

## 5. 活动页通用结构

状态：已确认。

### 5.1 顶部分段控件

位于导航栏下方，左右边距约 `16rpx`。

外框：

- 宽度接近全屏。
- 高度约 `84rpx`。
- 圆角约 `18rpx`。
- 背景黑色。
- 外边框 `1rpx solid #343434`。

四个分段：

- `我的活动`
- `销售数据`
- `实时订单`
- `核销管理`

active：

- 黄绿色背景 `#D7FF3F`。
- 黑色文字。
- 粗体。
- 圆角与外框接近。

inactive：

- 透明背景。
- 灰色文字。

### 5.2 大内容容器

分段控件下方约 `30rpx`。

- 左右边距约 `14rpx`。
- 背景：`#111111` 到 `#181818`。
- 圆角：约 `16rpx`。
- 最小高度按页面内容变化。

## 6. 活动页：我的活动

状态：已确认。

### 6.1 工具栏

位于大内容容器顶部，内边距约 `16rpx`。

左侧筛选按钮：

- 尺寸约 `88rpx x 88rpx`。
- 背景 `#202020`。
- 圆角 `14rpx`。
- icon：滑杆筛选，白色，约 `42rpx`。

搜索框：

- 高度约 `88rpx`。
- 左侧距筛选按钮约 `14rpx`。
- 背景 `#202020`。
- 圆角 `14rpx`。
- 左侧搜索 icon。
- placeholder：`搜索活动`。

右侧视图切换按钮组：

- 容器背景 `#202020`。
- 两个 icon：网格、列表。
- 默认网格 icon 更亮；列表 icon 灰色。
- 每个 icon 点击区域约 `76rpx`。

### 6.2 空态

空态在大内容容器中间偏上。

- 文案：`暂无活动`。
- 文案白色，约 `38rpx`，粗体。
- 下方按钮：`新增活动`。
- 按钮背景黄绿色。
- 按钮尺寸约 `168rpx x 88rpx`。
- 按钮文字黑色，粗体。

### 6.3 FAB

右下角悬浮 plus：

- 位置：底部 tab 上方，右边距约 `20rpx`。
- 尺寸约 `76rpx x 76rpx`。
- 背景黄绿色。
- 圆角约 `14rpx`，不是完全圆形。
- plus icon 黑色。

## 7. 筛选面板

状态：已确认。

触发：我的活动工具栏左侧筛选按钮。

层级：

- 弹出大面板覆盖页面内容。
- 背后内容变暗，但仍可看见顶部活动分段控件。
- 面板上边距约 `175rpx`，左右边距约 `30rpx`。

面板：

- 背景：`#242424`。
- 圆角：约 `12rpx`。
- 宽度：约 `690rpx`。
- 高度：接近屏幕高度，底部内容可滚动。

头部：

- 左侧筛选 icon。
- 标题：`筛选`，白色，约 `36rpx`，粗体。
- 右侧关闭 `X`。
- 头部下方分割线。

分组：

1. `审核状态`
   - `待发布`
   - `审核中`
   - `通过`
2. `渠道`
   - `微信小程序`
   - `抖音小程序`
3. `活动状态`
   - `上架`
   - `下架`
   - `结束`
4. `时间`
   - 时间控件未完整确认。

checkbox：

- 未选中：黑色小方框，灰色边框。
- 选中：黄绿色填充，黑色对勾。
- 选中项文字变黄绿色。

## 8. 活动页：销售数据

状态：已确认空态。

分段 active：`销售数据`。

工具栏：

- 第一行两个下拉框。
- 左下拉：`销售渠道`。
- 右下拉：`提现状态`。
- 两个下拉同宽，各占一半。
- 下拉框背景比容器稍深，边框弱灰，圆角约 `14rpx`。
- 第二行是全宽搜索框，placeholder `搜索活动`。

空态：

- 文案：`暂无活动`。
- 下方按钮：`新增活动`，黄绿色。

## 9. 活动页：实时订单

状态：已确认空态。

分段 active：`实时订单`。

工具栏：

- 左侧筛选按钮。
- 右侧搜索框 placeholder：`搜索订单`。
- 没有新增按钮。

空态：

- 文案：`暂无订单`。
- 文字白色居中。

## 10. 活动页：核销管理

状态：已确认列表态。

分段 active：`核销管理`。

工具栏：

- 左侧下拉框：`渠道`。
- 右侧搜索框：`搜索`。

列表容器：

- 大内容容器内纵向排列卡片。
- 卡片背景：`#202020`。
- 圆角：约 `14rpx`。
- 卡片内边距：约 `30rpx`。
- 卡片高度约 `280rpx`。

单张核销员卡片：

- 左上姓名：`刘`，白色，约 `34rpx`，粗体。
- 姓名下手机号：`13943079037`。
- 右上状态胶囊：`待激活`。
  - 背景深灰。
  - 文字黄绿色。
- 右上操作：
  - 二维码 icon。
  - 删除 icon。
- 下半区：
  - 左列 label `权限范围`，值 `活动`。
  - 右列 label `所属渠道`，值为渠道胶囊。
- 渠道胶囊：
  - `微信`：绿色微信 icon + `微信`。
  - `抖音`：抖音 icon + `抖音`。

FAB：

- 右下角黄绿色 plus，与我的活动一致。

## 11. 更多页

状态：已确认。

顶部三段分段控件：

- `活动合集`
- `活动分销`
- `活动抽奖`

外观与活动页分段控件一致，但只有三项。

### 11.1 活动合集

- 搜索框 placeholder：`搜索合集标题`。
- 空态文案：`暂无合集`。
- 右下角黄绿色 plus。

### 11.2 活动分销

- 搜索框 placeholder：`搜索分销活动名称`。
- 空态文案：`暂无分销活动`。
- 右下角黄绿色 plus。

### 11.3 活动抽奖

- 搜索框 placeholder：`搜索抽奖活动`。
- 空态文案：`暂无活动抽奖`。
- 右下角黄绿色 plus。

## 12. 账户页

状态：已确认。

### 12.1 品牌卡

顶部卡片：

- 背景：`#181818`。
- 圆角：约 `20rpx`。
- 左右边距：约 `15rpx`。
- 顶部内边距：约 `30rpx`。

内容：

- 左侧 logo：方形，约 `102rpx x 102rpx`，圆角约 `10rpx`。
- 名称：`PURE LOOP`，白色，约 `34rpx`。
- 认证行：黄绿色圆形对勾 + `已认证`。
- 中部分割线。
- 下方三列统计：
  - `入驻天数` / `200`
  - `当前等级` / `LV1`
  - `服务费比例` / `5%`

### 12.2 分组卡片

通用样式：

- 背景 `#181818`。
- 圆角约 `20rpx`。
- 分组标题白色，左上。
- 标题下方分割线。
- 每行高度约 `118rpx`。
- 左侧线性 icon。
- 中间白色文字。
- 右侧 chevron。

分组和行：

1. `基本信息`
   - `主办方编辑`
   - `主办方区域`
2. `付款信息`
   - `付费记录`
3. `账户信息`
   - `认证信息`
   - `提现信息`
4. `安全性`
   - `修改密码`
   - `退出登录`

底部 tab 会遮挡最后部分内容，列表需要足够底部 padding。

## 13. 发布活动流程

状态：第 1-5 步已确认。最终 `提交审核` 未点击。

### 13.1 Stepper

位于表单卡片顶部。

- 5 个节点：`活动信息 / 地址设定 / 上传海报 / 票券配置 / 活动资质`。
- 当前节点：
  - 黄绿色描边圆。
  - 内部黄绿色实心点。
  - 下方步骤名黄绿色。
- 已完成节点：
  - 黄绿色实心圆。
  - 内部黑色对勾。
  - 连线黄绿色。
- 未完成节点：
  - 深灰圆。
  - 内部深色小圆。
  - 连线深灰。

### 13.2 第 1 步：活动信息

已确认。

外层：

- 表单卡背景 `#111111`。
- 表单区域顶部紧贴 stepper 下方，有分割线。

字段样式：

- label 白色，约 `30rpx`，粗体。
- 输入框背景接近黑色。
- 输入框边框灰色，focus 时黄绿色，错误时红色。
- 输入框高度约 `76rpx`。
- 右侧计数形如 `0 / 80`，中间有空格。

字段：

1. `活动名称`
   - placeholder `请输入`
   - 计数 `0 / 80`
   - 错误 `请输入活动名称`
2. `分享标题`
   - placeholder `请输入`
   - 计数 `0 / 20`
   - 错误 `请输入分享标题`
3. `活动日期`
   - 默认值 `2026-05-13 20:08 ~ 2026-05-14 20:08`
   - 右侧日历 icon
4. `强实名模式`
   - 卡片背景 `#242424`
   - switch 关闭态：灰底白圆点
   - 说明文字灰色
5. `未成年人校验`
   - 同上
6. `活动概要`
   - 富文本编辑器高度大。
   - 工具栏两行 icon。
   - placeholder `请输入内容...`
   - 错误 `请填写活动概要`

右侧浮动预览：

- 黄绿色圆形按钮。
- 文案 `预览`。
- 固定在右侧中下方，不随字段宽度变化。

底部按钮：

- 第 1 步只有右侧 `下一步`。
- 背景黄绿色，黑字，圆角约 `16rpx`。

### 13.3 第 2 步：地址设定

已确认。

字段：

- `* 当前坐标地址`
- 默认值 `东长安街`

地图：

- 宽度接近表单卡内容宽。
- 高度约 `540rpx`。
- 深色网格地图。
- 左下角 `高德地图` 标识。
- 中央黄绿色 pin。
- pin 上方白色气泡：
  - 标题 `东长安街`
  - 副文本 `地址： 东长安街`
  - 右上 `x`

底部：

- 左 `上一步`，黑底描边。
- 右 `下一步`，黄绿色。
- 右侧仍有 `预览` 浮动按钮。

### 13.4 第 3 步：上传海报

已确认。

上传项布局：

- 每个上传项先显示 label，再显示虚线边框上传框。
- 上传框背景与页面近似，边框为灰色虚线。
- 上传框高度约 `260rpx`。
- 中央按钮 `上传`，黑色小圆角按钮，白字。
- 说明文字灰色居中。

上传项：

1. `活动详情页海报`
   - `适用于活动详情页展示，比例5:4 文件大小2M以下`
2. `活动详情长图`
   - `适用于活动页详情展示，文件大小2M以下`
   - 第二行 `显示在购票页面底部`
3. `活动列表及分享图`
   - `适用于活动列表及分享展示，比例4:3 文件大小2M以下`
4. `活动微信社群`
   - `适用于活动社群二维码展示，文件大小2M以下`

上传后：

- 上传框里显示图片缩略图。
- 详情页海报按竖图显示。
- 列表及分享图按横图显示。
- 缩略图周围仍保留虚线边框区域。

裁切弹层：

- 标题 `素材裁切`。
- 右上按钮 `上传图片`，黄绿色。
- 中间裁切区。
- 左上角显示裁切尺寸，例如 `636 x 509`。
- 补采截图中方形裁切显示 `634 x 634`。
- 裁切框四角蓝色控制点。
- 图片背景可在裁切框内平移/缩放，裁切框区域外不显示最终裁切范围。
- 底部左 `取消`，右 `确认`。
- 确认后显示 `上传中...` 遮罩。
- 我们自己的小程序必须实现这个裁切弹层和确认后的缩略图回显，不能只做系统选择文件或静态 mock。

校验 toast：

- 顶部浮层背景深灰。
- 左侧黄色感叹号图标。
- 标题 `温馨提示`，黄绿色。
- 文案按缺失素材变化。
- 已补采确认的缺失文案：
  - `请上传活动详情长图`
  - 早前已见过 `请上传列表页海报/活动分享图`
  - 两行结构为标题一行、正文一行，右上角有关闭 `x`

### 13.5 第 4 步：票券配置

已确认。

整体：

- stepper 前三步为黄绿色完成态，`票券配置` 为当前态，`活动资质` 灰色未完成。
- 页面黑底，内容在深灰大卡片中。
- 右下仍有浮动 `预览`。

销售渠道：

- 标题 `销售渠道`，白色加粗。
- 横向 checkbox：
  - `微信（必选）` 默认选中，方形黄绿色底，黑色对勾。
  - `抖音` 默认未选中，方形深色底，灰色边框。

规格配置：

- 标题 `规格配置`，右侧白色圆形 plus icon。
- 外层卡片深灰，描边，圆角约 `16rpx`。
- label `规格名称`。
- 输入框显示 `票务类型`，右侧计数 `4 / 15`。
- 小标题 `选项 1/5`。
- 已有选项框显示 `早鸟票`，右侧灰色垃圾桶 icon。
- 新增选项输入框 placeholder `其他选项`。
- 右侧黄绿色小按钮 `新增`。
- 底部右侧两个按钮：
  - `全部清除`：黑底描边，白字。
  - `保存`：黄绿色底，黑字。

规格详情：

- 标题 `规格详情`。
- 卡片第一行：左侧 `规格`，右侧 `早鸟票`。
- `开售时间` 日期范围输入：`2026-05-13 21:29 ~ 2026-05-14 20:08`，右侧日历 icon。
- `价格`：左侧 label，输入区域前有 `￥`，值 `0`。
- `库存`：值 `0`。
- `限购`：值 `0`。
- `观演人`：值 `1`。
- `启用/禁用`：右侧 switch，默认开启，轨道黄绿色，圆点白色。

底部按钮：

- 左 `上一步`：黑底描边。
- 右 `下一步`：黄绿色底，黑字。

交互要求：

- 点击 `下一步` 进入第 5 步，不触发提交。
- plus、新增、垃圾桶、全部清除、保存需要在我们自己的小程序中有明确状态反馈；没有接口时先做本地状态。

### 13.6 第 5 步：活动资质

已确认。

整体：

- stepper 前四步为完成态，`活动资质` 为当前态。
- 内容区域继续沿用当前管理后台深灰卡片和现有强调色映射，不直接照搬 `此刻霓虹` 黄绿色主色。
- 右下仍有浮动 `预览`。

提示卡：

- 深灰背景，圆角约 `16rpx`。
- 左侧黄绿色信息 icon。
- 标题 `微信渠道上架须知`，白色加粗。
- 正文：`演出类活动在微信小程序上架需要提交活动批文 其他活动可不提交。`

上传区：

- 标题：`活动批文资质（选填）`。
- 虚线边框上传框。
- 中央黑底描边按钮 `上传`。
- 说明第一行：`点击下载《活动批文资质模版》`，其中 `《活动批文资质模版》` 黄绿色，应表现为可点击链接。
- 说明第二行：`演出类活动需提交活动批文，文件大小2M以下。`

底部按钮：

- 左 `上一步`：黑底描边。
- 右 `提交审核`：黄绿色底，黑字。

最终提交审核不要在自动化测试中点击。

### 13.7 发布预览弹层

已确认。

触发：

- 发布流程第 1-5 步右下方都有黄绿色圆形浮动按钮 `预览`。

弹层：

- 深灰大弹层覆盖页面中部，下方仍可隐约看到后台底部 tab。
- 顶部左侧标题 `预览`。
- 顶部中间下拉选择器，文字截断为 `活动详...`，右侧向下箭头。
- 顶部右侧关闭 `x`。
- 内容区域是手机壳预览，手机壳外框黑色，圆角很大。

手机壳内：

- 顶部状态栏：`08:34`、信号、`4G`、电量。
- 左上返回箭头，右上微信胶囊。
- 首屏背景为上传的活动详情页海报，顶部偏黄绿色。
- 下半部有黑色渐变遮罩。
- 文案按草稿实时回显：
  - 标题 `test event`
  - 日期 `2026.05.13 - 14 周三 21:29-21:29`
  - 价格 `¥888`
  - 右侧分享 icon
  - 摘要 `test summary`

实现要求：

- 我们自己的小程序必须实现预览弹层，且使用当前表单状态驱动。
- 如果票价仍未填写，应按当前草稿或默认票种价格展示；不要使用写死图片替代。
- 下拉选择器后续可扩展到不同预览页面，但首版至少支持活动详情页。

## 14. 与当前实现的像素差距

当前实现需要修改的重点：

- 不要把 `$organizer-danger` 红色按钮改成 `此刻霓虹` 黄绿色；当前项目主操作色、FAB 和危险语义继续按现有 token 使用。
- 活动顶部 tab 不是文字 tab，而是带外框的分段控件。
- 首页右侧动作用斜向箭头，不是文字链接。
- 活动中心空态在大深灰容器中，不是独立居中在页面空白区。
- 更多页不是卡片网格，而是三段式空态工具页。
- 账户页不是资料行列表，而是品牌卡 + 分组设置。
- 发布流程底部 tab 仍显示；不要在发布流程中隐藏后台底栏，除非补充实机确认发现特定子状态隐藏。

## 15. 无视觉验收清单

让无视觉模型逐项核对 DOM/文本/样式：

- 页面视觉 token 仍引用当前 `$organizer-*`，没有新增 `$neo-*` 或硬编码 `#D7FF3F/#CFFF3F` 作为全局品牌主色。
- 当前项目已有的红色主操作按钮和 FAB 不被误改成 `此刻霓虹` 黄绿色。
- `活动` 页顶部文字顺序严格为 `我的活动 销售数据 实时订单 核销管理`。
- `更多` 页顶部文字顺序严格为 `活动合集 活动分销 活动抽奖`。
- `账户` 页必须出现 `PURE LOOP`、`已认证`、`入驻天数`、`当前等级`、`服务费比例`。
- 核销管理卡片必须出现 `权限范围`、`所属渠道`、`待激活`、二维码操作、删除操作。
- 发布第 1 步必须出现 `强实名模式`，不能只写 `实名模式`。
- 发布第 2 步必须出现 `当前坐标地址` 和 `东长安街` 默认值。
- 发布第 3 步必须出现四个上传项，且有 `素材裁切` 弹层、确认、上传中、缩略图回显。
- 第 4/5 步按本文档已确认结构实现；`提交审核` 只允许在用户明确确认后触发。
- 预览按钮必须在发布流程内可用，并打开草稿驱动的手机壳预览。
