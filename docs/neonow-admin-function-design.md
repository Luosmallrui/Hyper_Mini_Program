# 此刻霓虹管理后台复刻功能设计文档

采集时间：2026-05-13 20:48 CST

采集对象：微信小程序 `此刻霓虹NeoNow` 的 `管理后台`。

目标实现目录：`/Users/zijian_nong/Desktop/code/Hyper_Mini_Program/src/pages/user-sub/organizer`

视觉约束：复刻范围是功能、信息架构、字段、交互和校验；视觉风格必须跟我们当前小程序的 `管理后台` 保持一致。不要把 `此刻霓虹NeoNow` 的黄绿色品牌风格整套迁入本项目。样式 token 以当前 `/Users/zijian_nong/Desktop/code/Hyper_Mini_Program/src/pages/user-sub/organizer/index.scss` 的 `$organizer-*` 为准。

## 1. 结论摘要

当前我们自己的管理后台已经有基础壳：底部四个后台 tab、首页、活动中心、更多、账户，以及活动发布 5 步草稿流。但它更接近旧版蓝湖稿，不是实机确认到的 `此刻霓虹NeoNow` 后台。

需要优先补齐的差距：

1. 视觉系统不做品牌换肤：保留当前小程序管理后台的黑底、深灰卡片、`$organizer-danger: #FF3150` 主操作色、白色主按钮、12rpx 圆角和现有底部 tab 风格；只吸收 `此刻霓虹` 的布局层级、字段和交互。
2. 活动中心要重构为一个大容器内的工具栏/空态/列表视图，且支持网格/列表切换。
3. 销售数据、实时订单、核销管理不是我们当前的演示 mock 卡片样式，需要按实机的筛选、搜索、空态和核销员卡片重做。
4. `更多` 页当前是四宫格，实机是 `活动合集 / 活动分销 / 活动抽奖` 三段式运营工具。
5. `账户` 页当前是简化商户资料，实机是完整主办方账户中心：品牌卡、统计、基本信息、付款信息、账户信息、安全性。
6. 发布活动第 1-5 步已实机确认，与我们现有 wizard 结构相似但视觉、字段文案和交互完整度有差异；最终 `提交审核` 未触发，仍需作为危险动作隔离。
7. 素材上传不能只做占位：我们自己的小程序必须实现 `素材裁切`、上传回显和浮动 `预览`，预览要能展示活动详情页手机壳效果。

### 1.1 视觉映射原则

不要按 `此刻霓虹` 实机截图做品牌换肤。实现时按下面映射：

- 页面、导航、卡片、输入框、底部 tab：沿用当前 `$organizer-bg / $organizer-nav / $organizer-card / $organizer-panel / $organizer-input / $organizer-bottom-tab`。
- 主操作按钮、FAB、新增入口：沿用当前 `$organizer-danger` 或现有 `.primary-pill-button / .floating-plus-button`，不要改成 `此刻霓虹` 黄绿色。
- 发布流程 `下一步 / 提交审核`：沿用当前 `.white-pill-button` 的白底黑字，除非后续产品明确要求改色。
- 选中态、日期 active、局部强调、开启态 switch：优先复用当前已有的 `$organizer-calendar-active` 或当前组件选中态，不新建 `NeoNow` 主色。
- 删除、审核失败、危险动作：继续使用当前红色语义。
- 圆角、间距、字体粗细：优先跟当前管理后台保持一致；只在功能结构需要时调整布局。

## 2. 实机确认范围

已确认页面和状态：

- 首页空态。
- 活动 tab 下的 `我的活动` 空态、筛选面板、筛选选中态。
- 活动 tab 下的 `销售数据` 空态。
- 活动 tab 下的 `实时订单` 空态。
- 活动 tab 下的 `核销管理` 列表态。
- 更多 tab 下的 `活动合集`、`活动分销`、`活动抽奖` 空态。
- 账户 tab 首屏和下半屏。
- 新增活动流程第 1 步 `活动信息`。
- 新增活动流程第 2 步 `地址设定`。
- 新增活动流程第 3 步 `上传海报`，包含上传校验、素材裁切弹层、上传回显。
- 新增活动流程第 4 步 `票券配置`。
- 新增活动流程第 5 步 `活动资质`。
- 新增活动流程浮动 `预览` 弹层。

未确认页面和状态：

- 活动列表有数据态、销售数据有数据态、实时订单有数据态、活动合集/分销/抽奖有数据态。
- 所有提交、删除、退出登录、修改密码等会改变第三方账号状态的动作，包括第 5 步 `提交审核`。

实现时必须把未确认范围做成可扩展结构，但不要声称已经按 `此刻霓虹` 实机复刻完成。

## 3. 信息架构

管理后台使用原生导航标题 `管理后台`，页面内固定底部四个主 tab：

1. `首页`
2. `活动`
3. `更多`
4. `账户`

底部 tab 与顶部原生导航在所有主后台页保持可见。发布活动流程内，实机仍保留底部后台 tab，当前 active 为 `活动`。

### 3.1 首页

首页由三个纵向模块组成：

1. `已上架活动`
   - 左侧三横列表 icon。
   - 右上角斜向箭头，点击进入活动 tab 的 `我的活动`。
   - 空态文案：`暂无活动`。

2. `活动数据`
   - 左侧柱状图 icon。
   - 右上角斜向箭头，点击进入活动 tab 的 `销售数据`。
   - 小标题：`数据明细`。
   - 三个指标：`今日订单`、`今日销售`、`专区订阅量`。
   - 实机数值：`0.00`、`0.00`、`0.00`。

3. `快速配置`
   - 已补采确认共有 6 个入口：
     - `新增活动`，副文案 `在此处上架你最新的活动`，点击进入发布活动第 1 步。
     - `添加核销员`，副文案 `快速添加核销员`。
     - `活动抽奖`，副文案 `配合活动抽奖，增加社群活跃`。
     - `活动分销`，副文案 `加速您的活动传播`。
     - `活动合集`，副文案 `巡演必备，多场次集合展示`。
     - `赠票配置`，副文案 `一键赠票，方便快捷`。
   - 每个入口左侧是方形描边 icon 容器，右侧是 chevron。

### 3.2 活动

活动页内部使用顶部四段分段控件：

1. `我的活动`
2. `销售数据`
3. `实时订单`
4. `核销管理`

#### 我的活动

工具栏：

- 左侧筛选按钮。
- 搜索框 placeholder：`搜索活动`。
- 右侧视图切换按钮组：网格 icon、列表 icon。实机默认网格 icon 高亮。

空态：

- 大深灰容器中居中显示 `暂无活动`。
- 下方黄绿色按钮 `新增活动`。
- 右下角还有黄绿色悬浮 `+` 按钮。

筛选面板：

- 标题：`筛选`。
- 左侧是筛选 icon，右侧是关闭 `X`。
- 分组和选项：
  - `审核状态`：`待发布`、`审核中`、`通过`。
  - `渠道`：`微信小程序`、`抖音小程序`。
  - `活动状态`：`上架`、`下架`、`结束`。
  - `时间`：下方有时间选择控件，但实机只看到面板顶部，具体日期控件未完全确认。
- checkbox 未选中为黑底方框；选中后为黄绿色填充，黑色对勾，选项文字也变黄绿色。

#### 销售数据

工具栏：

- 第一行两个下拉筛选：
  - `销售渠道`
  - `提现状态`
- 第二行搜索框 placeholder：`搜索活动`。

空态：

- 大深灰容器居中 `暂无活动`。
- 下方黄绿色按钮 `新增活动`。

#### 实时订单

工具栏：

- 左侧筛选按钮。
- 搜索框 placeholder：`搜索订单`。

空态：

- 大深灰容器居中 `暂无订单`。
- 无新增按钮。

#### 核销管理

工具栏：

- 左侧下拉筛选：`渠道`。
- 右侧搜索框 placeholder：`搜索`。

列表卡片字段：

- 姓名：实机为 `刘`。
- 手机号：实机为 `13943079037`。
- 状态胶囊：`待激活`。
- 右侧操作 icon：
  - 二维码 icon，用于展示或分享核销员邀请码。
  - 删除 icon。删除是破坏性操作，开发或测试时必须二次确认。
- 下方字段：
  - 左侧 label `权限范围`，值 `活动`。
  - 右侧 label `所属渠道`，值为渠道胶囊。
  - 渠道胶囊可为 `微信` 或 `抖音`，带对应小 icon。

列表下方仍显示右下角黄绿色 `+` 悬浮按钮，用于新增核销员。

### 3.3 更多

更多页内部使用顶部三段分段控件：

1. `活动合集`
2. `活动分销`
3. `活动抽奖`

#### 活动合集

- 搜索框 placeholder：`搜索合集标题`。
- 空态：`暂无合集`。
- 右下角黄绿色 `+`。

#### 活动分销

- 搜索框 placeholder：`搜索分销活动名称`。
- 空态：`暂无分销活动`。
- 右下角黄绿色 `+`。

#### 活动抽奖

- 搜索框 placeholder：`搜索抽奖活动`。
- 空态：`暂无活动抽奖`。
- 右下角黄绿色 `+`。

当前我们自己的 `more/index.tsx` 是 `活动商家 / 场地排期 / 营销组件 / 品牌设置` 四宫格，应整体替换为以上三段式运营页。

### 3.4 账户

账户页由顶部品牌卡和分组列表组成。

品牌卡：

- logo 方图。
- 名称：`PURE LOOP`。
- 认证状态：绿色描边圆形对勾 + `已认证`。
- 三个统计：
  - `入驻天数`：`200`
  - `当前等级`：`LV1`
  - `服务费比例`：`5%`

分组：

1. `基本信息`
   - `主办方编辑`
   - `主办方区域`
2. `付款信息`
   - `付费记录`
3. `账户信息`
   - `认证信息`
   - `提现信息`
4. `安全性`
   - `修改密码`
   - `退出登录`

`修改密码`、`退出登录` 不要在自动化测试中触发。实现时需要二次确认或独立安全流程。

### 3.5 发布活动

发布流程为 5 步：

1. `活动信息`
2. `地址设定`
3. `上传海报`
4. `票券配置`
5. `活动资质`

已确认第 1-5 步如下。最终 `提交审核` 未点击。

#### 第 1 步：活动信息

字段顺序：

1. `活动名称`
   - placeholder：`请输入`
   - 计数：`0 / 80`
   - 必填，空值点击下一步显示红框和错误 `请输入活动名称`。
2. `分享标题`
   - placeholder：`请输入`
   - 计数：`0 / 20`
   - 必填，空值点击下一步显示红框和错误 `请输入分享标题`。
3. `活动日期`
   - 默认值实机为 `2026-05-13 20:08 ~ 2026-05-14 20:08`。
   - 右侧日历 icon。
4. `强实名模式`
   - 关闭态 switch。
   - 说明：`该模式开启后对整场活动生效，一个证件可以买到你设定的张数。`
5. `未成年人校验`
   - 关闭态 switch。
   - 说明：`开启该功能后用户下单默认输入身份证，18岁以下的未成年人将不能购票。`
6. `活动概要`
   - 富文本编辑器。
   - placeholder：`请输入内容...`
   - 工具栏 icon 包含撤销、重做、加粗、斜体、下划线、删除线、对齐、列表、文字颜色、表情、字号、清除格式等。
   - 必填，空值点击下一步显示红框和错误 `请填写活动概要`。

浮动按钮：

- 右侧中下方固定黄绿色圆形按钮 `预览`。

底部操作：

- 第 1 步只有右侧黄绿色 `下一步`。

#### 第 2 步：地址设定

字段：

- `地区`，上方字段未完整停留确认，但实机进入时可见其位于当前坐标地址上方。
- `* 当前坐标地址`
  - 默认值：`东长安街`。
  - 红色 `*` 表示必填。
- 地图预览
  - 高德地图样式网格。
  - 中央黄绿色 pin。
  - 白色气泡标题 `东长安街`，副文本 `地址：东长安街`，右上角有关闭 `x`。

底部操作：

- 左侧描边按钮 `上一步`。
- 右侧黄绿色按钮 `下一步`。
- 右侧仍有圆形 `预览`。

#### 第 3 步：上传海报

上传项：

1. `活动详情页海报`
   - 说明：`适用于活动详情页展示，比例5:4 文件大小2M以下`。
2. `活动详情长图`
   - 说明：`适用于活动页详情展示，文件大小2M以下 显示在购票页面底部`。
3. `活动列表及分享图`
   - 说明：`适用于活动列表及分享展示，比例4:3 文件大小2M以下`。
4. `活动微信社群`
   - 说明：`适用于活动社群二维码展示，文件大小2M以下`。

上传交互：

- 点击上传会打开 `素材裁切` 弹层。
- 弹层右上角按钮：`上传图片`。
- 中间是裁切区域，四角有蓝色控制点，左上角显示裁切尺寸，例如 `636 x 509`。
- 底部：左侧黑色 `取消`，右侧黄绿色 `确认`。
- 上传中显示覆盖层文字 `上传中...`。
- 上传成功后，上传框内显示缩略图。

校验：

- 点击下一步时，如果缺少素材，会显示顶部 toast：
  - `温馨提示`
  - `请上传列表页海报/活动分享图`
  - `请上传活动详情长图`
- 2026-05-13 21:13 补采确认：当前草稿已上传 `活动详情页海报` 和 `活动列表及分享图` 后，点击 `下一步` 仍停留第 3 步，toast 文案为 `温馨提示 / 请上传活动详情长图`。这说明 `活动详情长图` 是进入第 4 步的必传项。

第 4-5 步：

- 2026-05-13 21:29 用户手动补齐上传后已进入第 4 步和第 5 步。
- `活动详情长图` 是第 4 步前置必传项；`活动微信社群` 未阻断进入第 4 步。

#### 第 4 步：票券配置

页面结构：

- Stepper：`活动信息 / 地址设定 / 上传海报` 为完成态，`票券配置` 为当前态，`活动资质` 为未完成态。
- 顶部模块 `销售渠道`
  - `微信（必选）`：默认选中，黄绿色 checkbox。
  - `抖音`：默认未选中，灰色 checkbox。
- 模块 `规格配置`
  - 标题右侧有圆形 plus 图标，用于新增规格组。
  - 卡片内字段 `规格名称`，输入框默认 `票务类型`，右侧计数 `4 / 15`。
  - 小标题 `选项 1/5`。
  - 已有选项 chip/input：`早鸟票`，右侧垃圾桶图标可删除。
  - 新增选项输入框 placeholder：`其他选项`。
  - 黄绿色按钮 `新增`。
  - 底部右侧按钮：描边 `全部清除`、黄绿色 `保存`。
- 模块 `规格详情`
  - 卡片显示当前规格：左侧 label `规格`，右侧值 `早鸟票`。
  - `开售时间` 日期范围输入：`2026-05-13 21:29 ~ 2026-05-14 20:08`，右侧日历 icon。
  - `价格`：左侧 `￥`，输入值 `0`。
  - `库存`：输入值 `0`。
  - `限购`：输入值 `0`。
  - `观演人`：输入值 `1`。
  - `启用/禁用`：右侧 switch，默认开启，黄绿色轨道。
- 底部操作：左 `上一步`，右 `下一步`。
- 浮动 `预览` 仍在右下方。

#### 第 5 步：活动资质

页面结构：

- Stepper：前 4 步均为完成态，`活动资质` 为当前态。
- 顶部提示卡：
  - 左侧信息 icon。
  - 标题 `微信渠道上架须知`。
  - 正文 `演出类活动在微信小程序上架需要提交活动批文 其他活动可不提交。`
- 上传模块标题：`活动批文资质（选填）`。
- 上传框：
  - 灰色虚线边框。
  - 中央按钮 `上传`。
  - 文案 `点击下载《活动批文资质模版》`，其中模板名为黄绿色链接。
  - 说明 `演出类活动需提交活动批文，文件大小2M以下。`
- 底部操作：左 `上一步`，右黄绿色 `提交审核`。
- 浮动 `预览` 仍在右下方。

`提交审核` 是第三方账号状态改变动作，自动化测试和复刻验收时不要点击。

#### 发布流程预览

实机确认浮动 `预览` 会打开底部/居中深色弹层：

- 弹层标题 `预览`。
- 顶部中间有下拉选择器，当前显示截断 `活动详...`，右侧下箭头。
- 右上角关闭 `x`。
- 弹层内容是手机壳预览，模拟微信小程序活动详情页。
- 手机壳顶部显示状态栏时间 `08:34`、信号、`4G`、电量，内部还有微信胶囊和返回箭头。
- 预览图使用已上传海报作为首屏背景，并加黑色渐变遮罩。
- 预览内容按当前草稿回显：
  - 标题：`test event`
  - 日期：`2026.05.13 - 14 周三 21:29-21:29`
  - 价格：`¥888`
  - 分享 icon
  - 摘要：`test summary`

我们自己的小程序必须实现这个预览弹层；如果后端还未接入真实数据，也要先用当前表单草稿数据驱动预览，而不是静态图片。

## 4. 数据模型建议

### 4.1 Dashboard

```ts
type OrganizerDashboardTab = 'home' | 'activity' | 'more' | 'account'
```

### 4.2 活动中心

```ts
type ActivityInnerTab = 'mine' | 'sales' | 'orders' | 'verifiers'
type ActivityViewMode = 'grid' | 'list'
type Channel = 'wechat' | 'douyin'
type AuditStatus = 'draft' | 'pending' | 'approved'
type ActivityLifeStatus = 'up' | 'down' | 'ended'
```

活动筛选：

```ts
interface ActivityFilterState {
  auditStatuses: AuditStatus[]
  channels: Channel[]
  lifeStatuses: ActivityLifeStatus[]
  startAt: string
  endAt: string
}
```

核销员：

```ts
interface Verifier {
  id: string
  name: string
  phone: string
  permissionScope: 'activity'
  channel: Channel
  inviteStatus: 'pending' | 'active'
  qrCodeUrl?: string
}
```

### 4.3 更多页

```ts
type MoreInnerTab = 'collections' | 'distribution' | 'lottery'
```

每个 tab 都需要：

- keyword
- list
- empty state
- create action

### 4.4 账户

```ts
interface OrganizerAccount {
  name: string
  logo: string
  verified: boolean
  daysSinceJoined: number
  level: string
  serviceFeeRate: string
}
```

## 5. 当前代码差距清单

### 5.1 `home/index.tsx`

当前：

- 首页模块名称接近，但数据项是 `今日订单 / 今日销售 / 活动订阅量`。
- 快速配置是 `发布活动 / 添加核销员 / 票务配置 / 分销管理`。
- active 色和按钮色偏旧稿。

需要改：

- `活动订阅量` 改为 `专区订阅量`。
- `发布活动` 改为 `新增活动`。
- 首页卡片和按钮沿用当前 `$organizer-*` 视觉体系；只调整结构、文案和入口数量。
- 右上角箭头要是斜向箭头，不是文字链接 `前往活动中心/前往数据中心`。

### 5.2 `activities/index.tsx`

当前：

- 顶部 tab 是普通文字行。
- 我的活动空态按钮是红色。
- 销售数据展示 mock 指标卡和排行。
- 实时订单展示 mock 订单卡。
- 核销管理展示在线/离线状态的简化核销员。

需要改：

- 顶部改为胶囊分段控件。
- 我的活动工具栏增加视图切换。
- 销售数据工具栏改为 `销售渠道 / 提现状态 / 搜索活动`，空态结构与实机一致。
- 实时订单工具栏改为 `筛选 / 搜索订单`，空态文案 `暂无订单`。
- 核销管理重做为渠道筛选、搜索、核销员卡片、二维码、删除、新增。

### 5.3 `more/index.tsx`

当前：

- 四宫格卡片。

需要改：

- 三段控件：`活动合集 / 活动分销 / 活动抽奖`。
- 每段都有搜索框、空态和右下角 `+`。

### 5.4 `account/index.tsx`

当前：

- 静态商家资料和安全开关。

需要改：

- 顶部品牌卡。
- 基本信息、付款信息、账户信息、安全性四个分组。
- 移除或下沉当前 `重要操作短信提醒 / 审核失败邮件同步`，这些不是实机确认内容。

### 5.5 `index.tsx` 发布流程

当前：

- 步骤名称是 `活动信息 / 场地设定 / 上传海报 / 票券配置 / 活动资质`。
- 第 1 步字段大体接近，但 `实名模式` 文案不一致。
- 第 2 步用 mock 地图。
- 第 3 步是模拟上传。
- 第 4/5 步结构需要按实机补齐。
- 当前浮动预览若只是静态或缺失，需要改成草稿驱动的手机壳预览。

需要改：

- 步骤名称第二步改为 `地址设定`。
- `实名模式` 改为 `强实名模式`，并同步实机说明文案。
- `活动详情长图` 的说明文案按实机改。
- 上传必须有素材裁切弹层、上传中、缩略图回显；若先做 mock，也要复刻裁切弹层结构，不能只做选择文件占位。弹层视觉使用当前管理后台按钮、边框、卡片 token。
- 第 4 步按实机补齐 `销售渠道 / 规格配置 / 规格详情`。
- 第 5 步按实机补齐 `微信渠道上架须知 / 活动批文资质（选填） / 提交审核`。
- 浮动 `预览` 必须打开预览弹层，使用当前表单草稿渲染活动详情页手机壳效果。

## 6. 建议实施顺序

1. 固化当前管理后台设计 token：继续使用 `$organizer-bg`、`$organizer-card`、`$organizer-danger`、`$organizer-white-button`、`$organizer-calendar-active` 等现有 token，不新增 `NeoNow` 品牌色体系。
2. 重构 organizer 页面状态模型：主 tab + activity inner tab + more inner tab。
3. 先完成首页、活动中心空态、筛选面板、更多三段页和账户页，因为这些已实机确认。
4. 重构核销管理列表，这是功能差距最大的已确认模块。
5. 改发布活动第 1-5 步，并优先完成素材裁切、上传回显和预览弹层。
6. 接入或 mock 第 4 步票券配置的数据结构，确保新增/删除/清空/保存和启用开关状态完整。
7. 最后做接口占位和 mock fallback。

## 7. 验收标准

基础验收：

- `npm run build:weapp -- --mode development` 通过。
- `npx tsc --noEmit` 通过。
- `npm run check:encoding` 通过。

功能验收：

- 四个底部 tab 可切换。
- 活动 tab 内四个分段可切换。
- 更多 tab 内三个分段可切换。
- 我的活动筛选面板可打开、关闭、勾选、取消勾选。
- 搜索框均能输入和清空。
- 发布第 1 步必填校验文案与实机一致。
- 发布第 2 步地图和默认地址结构与实机一致。
- 发布第 3 步上传裁切弹层、上传中、缩略图回显与实机一致。
- 发布第 4 步销售渠道、规格配置、规格详情、开售时间、价格、库存、限购、观演人、启用开关与实机一致。
- 发布第 5 步微信渠道须知、可选批文上传、模板下载链接、提交审核按钮与实机一致。
- 浮动预览弹层可打开和关闭，下拉器、手机壳、状态栏、海报背景、标题、日期、价格、摘要等字段由当前草稿驱动。

安全验收：

- 不自动触发删除核销员。
- 不自动触发退出登录。
- 不自动提交活动审核。
- 不自动修改密码或进入最终改密提交。
